# 競輪AI予測 (keirin-ai-predictor)

LightGBM LambdaRankモデルによる競輪レースの着順予測システム。

**ダッシュボード**: https://haruqube.github.io/keirin-ai-predictor/

## 予測精度（過去6ヶ月 2,799レース検証）

| 指標 | 値 |
|------|------|
| Top1的中率 | **47.8%** |
| Top1が3着以内 | **78.8%** |
| Top3平均的中数 | **1.90/3** |

### 信頼度スコアの効果

信頼度スコア（1位vs3位のスコア差）で予測の確からしさを判定：

| 信頼度 | 条件 | レース割合 | Top1的中率 | Top1が3着以内 |
|--------|------|-----------|-----------|-------------|
| 高信頼 | ≥ 2.3 | 15% | **86.7%** | **97.9%** |
| 中信頼 | ≥ 0.85 | 55% | 46.1% | 78.5% |
| 低信頼 | < 0.85 | 30% | 31.4% | 63.9% |

> 6ヶ月2,799レースの分析で10種類の信頼度指標を比較検証。
> 1位vs3位スコア差が最も安定した相関（r=0.365, p<0.001）を示した。

## プロジェクト構成

```
keirin-ai-predictor/
├── data/               # スクレイパー・キャッシュ
│   ├── scraper.py      # netkeirinスクレイパー
│   └── cache/          # HTMLキャッシュ（~15,000ファイル）
├── db/                 # SQLiteデータベース
│   ├── schema.py       # テーブル定義・CRUD
│   └── keirin.db       # 63,508レース・2,673選手
├── features/           # 特徴量エンジニアリング
│   ├── builder.py      # 統合ビルダー（38特徴量）
│   ├── rider_features.py   # 選手成績特徴量
│   ├── race_features.py    # レース条件特徴量
│   └── line_features.py    # ライン構成特徴量
├── models/             # 機械学習モデル
│   ├── lgbm_ranker.py  # LightGBM LambdaRank
│   └── base.py         # 基底クラス
├── scripts/            # 実行スクリプト
│   ├── predict_races.py    # 日次予測（ライン補正込み）
│   ├── train_model.py      # モデル学習
│   ├── init_db.py          # データ収集
│   └── sync_to_supabase.py # Supabase同期
├── publishing/         # 記事・SNS連携
│   └── templates/      # Jinja2テンプレート
├── docs/               # GitHub Pages
│   └── index.html      # ダッシュボード（SPA）
├── config.py           # 設定・定数
└── README.md
```

## データパイプライン

```
netkeirin → スクレイピング → SQLite → 特徴量構築 → LGBMモデル → 予測
                                                                  ↓
                              Supabase ← 同期 ← 予測結果 → ライン補正
                                  ↓
                            GitHub Pages ダッシュボード
```

## 技術スタック

- **モデル**: LightGBM LambdaRank（ランキング学習）
- **特徴量**: 38次元（選手成績14 + 上がりタイム3 + バンク相性2 + フォーム4 + レース条件5 + ライン6 + 相対順位2 + 交互作用2）
- **学習データ**: 2022-2025年 63,508レース
- **データソース**: netkeirin (keirin.netkeiba.com)
- **DB**: SQLite（ローカル）→ Supabase PostgreSQL（フロントエンド用）
- **フロントエンド**: バニラJS SPA（GitHub Pages）

## 主要な特徴

### ライン補正
競輪特有の「ライン戦」を反映。出走表のライン構成を解析し、予測スコアに補正を加える。

| ライン構成 | 役割 | 補正値 |
|-----------|------|--------|
| 3人ライン | 番手 | +0.30 |
| 3人ライン | 自力 | -0.20 |
| 3人ライン | 3番手 | -0.30 |
| 最強ライン所属 | — | +0.20 |

### ダッシュボード機能
- 日付 → 会場 → レース一覧 → 詳細の4階層ナビゲーション
- LIVE / 発走前 / 終了のリアルタイムステータス表示
- LIVE開催場優先ソート
- 信頼度バッジ（高/中/低）
- おすすめレース自動抽出（高信頼レースのみ）

### 会場自動検出
netkeirin トップページから当日開催中の全会場を自動検出。全43場対応。

## 使い方

### 予測実行
```bash
# 今日のレース予測（開催場自動検出）
python scripts/predict_races.py --date 20260320

# 特定の会場を指定
python scripts/predict_races.py --date 20260320 --velodromes 22,87

# Supabaseに同期
python scripts/sync_to_supabase.py --since 2026-03-20
```

### モデル学習
```bash
python scripts/train_model.py
```

## 信頼度指標の分析詳細

2,799レース（2025/09/20〜2026/03/19）で10種類の信頼度指標を比較：

| 指標 | 相関(r) | High-Low差 |
|------|---------|-----------|
| 現行(min(top3)-max(残)) | +0.055 | +6.8% |
| 1位vs2位差 | +0.350 | +32.3% |
| **1位vs3位差** | **+0.365** | **+35.4%** |
| 1位vs4位差 | +0.370 | +35.4% |
| エントロピー | +0.371 | +34.0% |
| スコア標準偏差 | +0.341 | +29.9% |

1位vs3位差を採用した理由：
- 相関・H-L差ともにトップクラス
- 3ヶ月/6ヶ月とも安定（r=0.35前後で変動なし）
- 計算が単純で解釈しやすい
